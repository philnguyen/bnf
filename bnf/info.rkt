#lang info

(define collection "bnf")

(define deps '("base"
               "typed-racket-lib"
               "typed-racket-more"))

(define pkg-desc "Macro for declaring data in BNF")

(define pkg-authors '(pcn))
